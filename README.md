# 🇷🇺 RUDWEAK v1.10 (Offline Edition)

**RUDWEAK** — это оптимизированная версия SDWEAK, полностью переработанная для локальной установки.
**Больше не требуется интернет или VPN во время установки!** Все необходимые пакеты уже включены в архив.

---

## 🚀 Основные возможности

*   **⚡ Максимальная производительность:** Ядро Linux Charcoal, оптимизации GPU и CPU.
*   **🛠 Полная автономность:** Установка проходит без подключения к сети.
*   **🛡 Безопасность:** Проверка модели устройства (LCD/OLED) для предотвращения ошибок.
*   **🇷🇺 Локализация:** Полная поддержка русского языка.

---

## 📋 Требования

1.  **Steam Deck** (LCD или OLED).
2.  **SteamOS 3.5+** (Рекомендуется 3.6/3.7).
3.  **Свободное место:** Минимум **500 МБ** в домашней папке (`/home`).
4.  **Удаление конфликтов:** Рекомендуется удалить *CryoUtilities*, если он установлен.

---

## 💾 Установка

### 1. Подготовка
Перейдите в режим рабочего стола (Desktop Mode).
Если вы еще не задали пароль суперпользователя (sudo), откройте терминал (Konsole) и введите:
```bash
passwd
```
*(Введите пароль дважды. Символы при вводе не отображаются).*

### 2. Распаковка
Распакуйте папку `RUDWEAK` из архива в любое удобное место (например, на Рабочий стол или в Загрузки).

### 3. Запуск
Откройте папку `RUDWEAK` и запустите файл:
👉 **`RUDWEAK-installer.desktop`** (выберите "Execute" / "Запустить").

**Альтернативный способ (через терминал):**
Нажмите правой кнопкой мыши внутри папки `RUDWEAK` -> "Open Terminal Here" и введите:
```bash
./install.sh
```

---

## ⚙️ Рекомендуемые настройки при установке

Мастер установки задаст несколько вопросов. Рекомендуемые ответы:

1.  **Оптимизированное ядро (Linux Charcoal)?** -> **Y** (Да)
2.  **Фикс фреймтайма (LCD)?** -> **Y** (Да) *[Автоматически скрыто для OLED]*
3.  **Разгон экрана до 70Hz (LCD)?** -> **По желанию** *[Автоматически скрыто для OLED]*
4.  **Энергоэффективность CPU?** -> **N** (Нет) для тяжелых игр, **Y** (Да) для эмуляторов/инди.
5.  **Оптимизации GPU?** -> **Y** (Да)

---

## 🗑 Удаление

Чтобы вернуть стоковую конфигурацию:
1.  Зайдите в папку `RUDWEAK`.
2.  Запустите `uninstall.sh` через терминал:
    ```bash
    ./uninstall.sh
    ```
    Или просто запустите файл `uninstall.sh`, если он исполняемый.

---

## ❓ FAQ

**Q: Нужен ли интернет?**
A: Нет! Это полностью оффлайн-версия.

**Q: Безопасно ли это для OLED версии?**
A: Да. Скрипт автоматически определяет модель Steam Deck (Galileo/OLED) и отключает опасные твики (разгон экрана и старые фиксы фреймтайма), применяя только безопасные системные оптимизации.

**Q: Почему архив весит больше обычного?**
A: Потому что внутри уже лежат все пакеты ядра, драйверы и утилиты. Вам не нужно ничего качать.

---

**Credits:**
*   Based on SDWEAK by @noncatt
*   RUDWEAK Fork & Offline Adaptation by @norevived
